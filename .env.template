# scanner/.env.template
# Copy to .env and fill in values. Bun auto-loads .env at startup.
# ──────────────────────────────────────────────────────────────────

# Project root — where the scanner discovers project directories
BUN_PLATFORM_HOME="/Users/nolarose/Projects"

# Recommended Bun runtime settings (for performance & consistency across projects)
# ------------------------------------------------------------------------------
# Centralize transpiler cache (files >50KB) under your platform home — git-ignored
# This speeds up repeated scans/builds and keeps home dir clean
BUN_RUNTIME_TRANSPILER_CACHE_PATH=${BUN_PLATFORM_HOME}/.bun-cache

# Disable telemetry & crash report uploads (privacy best practice)
# DO_NOT_TRACK=1

# Auth — registry token for scoped package access
FW_REGISTRY_TOKEN=""

# Registry override — leave empty to use per-project bunfig.toml settings
BUN_CONFIG_REGISTRY=""

# DNS cache TTL — AWS recommends 5s for dynamic environments (Bun default: 30)
# BUN_CONFIG_DNS_TIME_TO_LIVE_SECONDS=5

# Network & script concurrency tuning
BUN_CONFIG_NETWORK_CONCURRENCY=256
BUN_CONFIG_CONCURRENT_SCRIPTS=16
